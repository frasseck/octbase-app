[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    no_stack: test needs no running Octbase instance (drives conftest helpers directly)
